node_modules/
.env

# Credenciales OAuth de Google Search Console (NUNCA subir al repo)
client_secrets.json
token.json
credentials/

# Config MCP real con secretos (usa .mcp.json.example como plantilla)
.mcp.json
